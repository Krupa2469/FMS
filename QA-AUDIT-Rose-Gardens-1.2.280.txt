Rose Gardens v1.2.280 QA Audit
Date: 2026-09-09

Scope
- Resident Colony Payments -> Open PhonePe / Open GPay
- Preserve v1.2.279 Resident My Profile fixes

Root cause corrected
- v1.2.279 removed PackageManager launcher flags and used startActivityForResult() to open the payer app launcher.
- On some Android/OEM/payment-app combinations that handoff could silently fail after the beneficiary mobile was copied, leaving only Android's "Copied" indication.

v1.2.280 correction
- PhonePe package: com.phonepe.app.
- Google Pay package: com.google.android.apps.nbu.paisa.user.
- Android resolves the installed payer app launcher through PackageManager.
- If getLaunchIntentForPackage() is unavailable, MAIN/LAUNCHER is resolved explicitly.
- PackageManager's launcher flags are preserved.
- Payer app is opened with normal startActivity(), not startActivityForResult().
- The launch result is returned to JavaScript only after Android attempts startActivity().
- Beneficiary mobile 9704035400 is still copied for Pay to Mobile Number.
- Android Back/return restores Rose Gardens through the existing onResume payment-handoff callback.
- If an app is not installed/cannot be opened, a clear error is displayed.
- Normal Web browser use clearly explains that direct package launching is an Android-app capability.

Retained payment account
- UPI ID: sriharipriya1209@okhdfcbank
- PhonePe/GPay No.: +91 9704035400
- Both identifiers belong to the same beneficiary account.

Retained Resident Profile fix
- Native resident token refresh and direct authenticated Firestore self-profile update remain unchanged.

Validation
- Web JavaScript: 34 files checked, 0 syntax failures.
- Web HTML: 20 files parsed successfully.
- Android XML: 5 files parsed successfully.
- Web and Android embedded Web assets: synchronized byte-for-byte.
- Android versionCode: 280.
- Android versionName: 1.2.280.
- Gradle APK compilation could not run because Gradle 8.11.1 is not cached and services.gradle.org is unreachable from the sandbox.
