Rose Gardens Web 1.2.260 — Fix 42

Office-bearer/Admin login session correction:
- Added the missing activateOfficerSession callable to the active functions/index.js.
- Callable runs in asia-south1 with CORS enabled.
- The callable resolves Role Master profiles even when the document ID is an RGRWA account ID rather than the Firebase Auth UID.
- It validates the signed-in Firebase email and office-bearer role, then issues a server-controlled role custom claim.
- Browser login force-refreshes the Firebase ID token before protected Firestore reads.
- Firestore rules accept validated staff/resident claims while preserving the existing Role Access permissions.
- Added a narrow same-email userProfiles read recovery path for historical Role Master IDs.

Required deployment for this fix:
  export FUNCTIONS_DISCOVERY_TIMEOUT=60
  firebase deploy --only functions:activateOfficerSession
  firebase deploy --only firestore:rules
  firebase deploy --only hosting
